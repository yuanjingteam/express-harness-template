pre-commit:
  parallel: true
  env:
    PATH: $HOME/.volta/bin:$PATH
  commands:
    lint:
      run: npx eslint src/ test/ --ext .ts
      glob: "*.{ts,tsx}"
    typecheck:
      run: npx tsc --noEmit
      glob: "*.{ts,tsx}"

pre-push:
  commands:
    check:
      run: npm run check

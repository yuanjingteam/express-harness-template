pre-commit:
  parallel: true
  commands:
    lint:
      run: npx eslint src/ test/ --ext .ts
      glob: "*.{ts,tsx}"
    typecheck:
      run: npx tsc --noEmit
      glob: "*.{ts,tsx}"

pre-push:
  commands:
    check:
      run: npm run check
